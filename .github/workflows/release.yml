# Release pipeline — manually triggered build of APK + IPA, attached to a
# GitHub Release. Designed to work zero-config: with no secrets defined,
# Android produces a debug-signed APK and iOS produces an unsigned IPA
# (suitable for sideloading via AltStore / Sideloadly / a jailbroken
# device). Adding the optional secrets listed below upgrades both to
# fully production-signed artifacts.
#
# Trigger UI: Actions tab → "Release (manual)" → Run workflow.
#
# Optional secrets:
#   ANDROID_KEYSTORE_BASE64   base64 of your .jks
#   ANDROID_KEYSTORE_PASSWORD store password
#   ANDROID_KEY_ALIAS         key alias
#   ANDROID_KEY_PASSWORD      key password
#
#   IOS_CERTIFICATE_BASE64    base64 of your .p12 distribution cert
#   IOS_CERTIFICATE_PASSWORD  password the .p12 was exported with
#   IOS_PROVISIONING_PROFILE_BASE64  base64 of your .mobileprovision
#   IOS_TEAM_ID               your Apple Team ID (10-char)
#   IOS_EXPORT_METHOD         app-store-connect | ad-hoc | development
#                             (default: development — safest for sideload)

name: Release (manual)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.2.0). Will be created if missing.'
        required: true
        default: 'v0.0.0-dev'
      prerelease:
        description: 'Mark as pre-release?'
        type: boolean
        default: true
      build_android:
        description: 'Build Android APK?'
        type: boolean
        default: true
      build_ios:
        description: 'Build iOS IPA?'
        type: boolean
        default: true
      flutter_version:
        description: 'Flutter SDK version'
        required: true
        default: '3.32.0'

concurrency:
  # One release build at a time per tag — avoids racing artifact uploads.
  group: release-${{ github.event.inputs.tag }}
  cancel-in-progress: false

jobs:
  # ───────────────────────── Android ─────────────────────────
  android:
    if: ${{ github.event.inputs.build_android == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      apk_name: ${{ steps.find_apk.outputs.name }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ github.event.inputs.flutter_version }}
          channel: stable
          cache: true

      # If a release-signing keystore is provided as a secret, decode it
      # into the project and write a `key.properties` that the Gradle
      # config can consume. Without these secrets we just leave the
      # debug-signing config in place (the build still succeeds, the
      # APK is just dev-signed — fine for internal QA, NOT for the
      # Play Store).
      - name: Configure Android signing (optional)
        if: ${{ env.HAS_ANDROID_SIGNING == 'true' }}
        env:
          HAS_ANDROID_SIGNING: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
          KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          STORE_PASS: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASS: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          mkdir -p android/app
          echo "$KEYSTORE_B64" | base64 -d > android/app/upload.keystore
          cat > android/key.properties <<EOF
          storePassword=$STORE_PASS
          keyPassword=$KEY_PASS
          keyAlias=$KEY_ALIAS
          storeFile=upload.keystore
          EOF

      - name: Pub get
        working-directory: .
        run: flutter pub get

      - name: Analyze (non-fatal)
        working-directory: .
        run: flutter analyze --no-fatal-infos || true

      - name: Build APK
        working-directory: .
        # split-per-abi gives us 3 smaller APKs (armv7 / arm64 / x86_64).
        # Chinese stores often have 100 MB free-tier upload limits so
        # this is the friendlier shape than one fat universal APK.
        run: flutter build apk --release --split-per-abi

      - name: Find APKs
        id: find_apk
        working-directory: .
        run: |
          ls -lah build/app/outputs/flutter-apk/
          # Rename arm64 to a clean, tag-aware filename for the release
          # asset list. The others stay as-is.
          TAG="${{ github.event.inputs.tag }}"
          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
             "explore_journal-${TAG}-android-arm64.apk"
          cp build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk \
             "explore_journal-${TAG}-android-arm32.apk" || true
          cp build/app/outputs/flutter-apk/app-x86_64-release.apk \
             "explore_journal-${TAG}-android-x86_64.apk" || true
          ls -lah explore_journal-${TAG}-android-*.apk
          echo "name=explore_journal-${TAG}-android" >> "$GITHUB_OUTPUT"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apks
          path: ./explore_journal-*-android-*.apk
          if-no-files-found: error
          retention-days: 14

  # ─────────────────────────── iOS ───────────────────────────
  ios:
    if: ${{ github.event.inputs.build_ios == 'true' }}
    runs-on: macos-14
    timeout-minutes: 45
    outputs:
      ipa_name: ${{ steps.find_ipa.outputs.name }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ github.event.inputs.flutter_version }}
          channel: stable
          cache: true

      - name: Pub get + iOS pod install
        working-directory: .
        run: |
          flutter pub get
          cd ios && pod install --repo-update

      # When signing secrets exist, install the cert into a temp
      # keychain and the provisioning profile into the standard
      # Library/MobileDevice path. xcodebuild can then archive + export
      # a real IPA. When they're missing, we fall back to the unsigned
      # `flutter build ios --no-codesign` flow which still produces an
      # IPA-like .app bundle suitable for sideloading.
      - name: Install code-signing assets (optional)
        if: ${{ env.HAS_IOS_SIGNING == 'true' }}
        env:
          HAS_IOS_SIGNING: ${{ secrets.IOS_CERTIFICATE_BASE64 != '' }}
          CERT_B64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
          CERT_PASS: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          PROFILE_B64: ${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}
        run: |
          set -euo pipefail
          KEYCHAIN=runner.keychain
          KEYCHAIN_PASS=$(uuidgen)
          security create-keychain -p "$KEYCHAIN_PASS" "$KEYCHAIN"
          security default-keychain -s "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASS" "$KEYCHAIN"
          security set-keychain-settings -lut 7200 "$KEYCHAIN"

          echo "$CERT_B64" | base64 -d > cert.p12
          security import cert.p12 -k "$KEYCHAIN" -P "$CERT_PASS" \
            -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: \
            -s -k "$KEYCHAIN_PASS" "$KEYCHAIN"
          rm cert.p12

          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          echo "$PROFILE_B64" | base64 -d \
            > "$HOME/Library/MobileDevice/Provisioning Profiles/build.mobileprovision"

      - name: Build IPA (unsigned)
        if: ${{ secrets.IOS_CERTIFICATE_BASE64 == '' }}
        working-directory: .
        # Produces build/ios/iphoneos/Runner.app (no signature). We zip
        # the .app inside a Payload/ tree so the result is .ipa-shaped
        # and sideloaders (AltStore, Sideloadly) can re-sign it on
        # install. This is the only legal way to ship an iOS build
        # without an Apple Developer cert in CI.
        run: |
          set -euo pipefail
          flutter build ios --release --no-codesign
          mkdir -p Payload
          cp -R build/ios/iphoneos/Runner.app Payload/
          TAG="${{ github.event.inputs.tag }}"
          zip -qr "explore_journal-${TAG}-ios-unsigned.ipa" Payload
          ls -lah explore_journal-${TAG}-ios-unsigned.ipa

      - name: Build IPA (signed)
        if: ${{ secrets.IOS_CERTIFICATE_BASE64 != '' }}
        working-directory: .
        env:
          EXPORT_METHOD: ${{ secrets.IOS_EXPORT_METHOD || 'development' }}
          TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
        run: |
          set -euo pipefail
          # Write an ExportOptions.plist that matches the secret's
          # method/team. Manual signing keeps the workflow honest about
          # what cert + profile it's actually using.
          cat > /tmp/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>method</key><string>${EXPORT_METHOD}</string>
            <key>teamID</key><string>${TEAM_ID}</string>
            <key>signingStyle</key><string>automatic</string>
            <key>stripSwiftSymbols</key><true/>
            <key>uploadSymbols</key><false/>
          </dict></plist>
          EOF
          flutter build ipa --release --export-options-plist=/tmp/ExportOptions.plist
          TAG="${{ github.event.inputs.tag }}"
          # `flutter build ipa` emits exactly one .ipa under build/ios/ipa/.
          cp build/ios/ipa/*.ipa "explore_journal-${TAG}-ios.ipa"
          ls -lah explore_journal-${TAG}-ios.ipa

      - name: Locate IPA
        id: find_ipa
        working-directory: .
        run: |
          ls -lah explore_journal-*-ios*.ipa
          echo "name=explore_journal-${{ github.event.inputs.tag }}-ios" >> "$GITHUB_OUTPUT"

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: ./explore_journal-*-ios*.ipa
          if-no-files-found: error
          retention-days: 14

  # ───────────────────── Publish GitHub Release ─────────────────────
  release:
    needs: [android, ios]
    # Run as long as at least one platform job succeeded; tolerate the
    # other being skipped (because the user unticked it) or failed
    # (so the user still gets the platform that did build).
    if: ${{ always() && (needs.android.result == 'success' || needs.ios.result == 'success') }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download Android APKs
        if: ${{ needs.android.result == 'success' }}
        uses: actions/download-artifact@v4
        with:
          name: android-apks
          path: artifacts

      - name: Download iOS IPA
        if: ${{ needs.ios.result == 'success' }}
        uses: actions/download-artifact@v4
        with:
          name: ios-ipa
          path: artifacts

      - name: List artifacts
        run: ls -lah artifacts/

      - name: Compose release notes
        id: notes
        run: |
          cat > /tmp/notes.md <<EOF
          # Explore Journal ${{ github.event.inputs.tag }}

          Manually-built release. Artifacts attached below.

          ## Android
          - \`explore_journal-${{ github.event.inputs.tag }}-android-arm64.apk\` — modern devices (recommended)
          - \`explore_journal-${{ github.event.inputs.tag }}-android-arm32.apk\` — older 32-bit phones
          - \`explore_journal-${{ github.event.inputs.tag }}-android-x86_64.apk\` — emulator / x86 Chromebooks

          ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' && '**Production-signed** with the upload keystore.' || 'Debug-signed — install via `adb install -r` or sideload. NOT submittable to Play Store as-is.' }}

          ## iOS
          - \`explore_journal-${{ github.event.inputs.tag }}-ios*.ipa\`

          ${{ secrets.IOS_CERTIFICATE_BASE64 != '' && '**Production-signed** for ' || 'Unsigned — install via AltStore / Sideloadly / TrollStore. ' }}${{ secrets.IOS_CERTIFICATE_BASE64 != '' && secrets.IOS_EXPORT_METHOD || 'NOT submittable to App Store as-is.' }}

          ---

          Built from ${{ github.sha }} on Flutter ${{ github.event.inputs.flutter_version }}.
          See \`docs/publishing.md\` for store-submission guidance.
          EOF
          cat /tmp/notes.md

      - name: Create / update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: ${{ github.event.inputs.tag }}
          body_path: /tmp/notes.md
          prerelease: ${{ github.event.inputs.prerelease == 'true' }}
          # Append assets — re-runs of the same tag won't fail if files
          # already exist.
          fail_on_unmatched_files: false
          files: |
            artifacts/*.apk
            artifacts/*.ipa
