<?xml version="1.0" encoding="utf-8"?>
<!--
  Production posture: HTTPS only by default. Cleartext HTTP is permitted
  globally at the Android level because users plug in arbitrary LAN IPs
  for WebDAV / image hosts that cannot be enumerated at build time. The
  REAL protection is in the runtime HTTP client (see
  `services/security/http_guard.dart`) which refuses cleartext to any
  non-private destination.

  Trust anchors: system CAs only — user-installed CAs are NOT trusted
  in release builds, which is what stops a tampered phone or corporate
  MITM proxy from intercepting our traffic.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
