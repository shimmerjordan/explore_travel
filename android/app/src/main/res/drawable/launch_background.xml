<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash: night-navy matching the app's dark map veil, so the
     cold-start sequence goes 夜色启动屏 → 雾罩地图, never a white flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <color android:color="#0F1E28" />
    </item>
</layer-list>
